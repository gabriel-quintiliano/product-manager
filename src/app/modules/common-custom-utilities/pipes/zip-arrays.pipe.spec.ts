import { ZipArraysPipe } from './zip-arrays.pipe';

describe('ZipArraysPipe', () => {
  it('create an instance', () => {
    const pipe = new ZipArraysPipe();
    expect(pipe).toBeTruthy();
  });
});
