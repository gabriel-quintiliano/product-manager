import { ZipObjectsPipe } from './zip-objects.pipe';

describe('ZipObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new ZipObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
